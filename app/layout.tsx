import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'SGU PERA PREMIER CHAMPIONSHIP 2025',
  description: 'The PERA Premier Championship is a state-level inter-university sports meet organized by the Preeminent Education and Research Association (PERA)',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
